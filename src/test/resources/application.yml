server:
  port : 8080

management:
  endpoint:
    health:
      show-details: always
    metrics:
      enabled: true
    prometheus:
      enabled: true
spring:
  data:
    mongodb:
      authentication-database=mydatabase: admin
      database: test
      port: 27017
      username: test
      password: password
      host: localhost